# name: generate animation snake

# on:
#   # run automatically every 24 hours
#   schedule:
#     - cron: "0 */12 * * *" 
  
#   # allows to manually run the job at any time
#   workflow_dispatch:
  
#   # run on every push on the main branch
#   push:
#     branches:
#     - main

# permissions:
#   contents: write
  
# jobs:
#   generate:
#     runs-on: ubuntu-latest
#     timeout-minutes: 10
    
#     steps:
#       # generates a snake game from a github user (<github_user_name>) contributions graph, output a svg animation at <svg_out_path>
#       - name: generate github-contribution-grid-snake.svg
#         uses: Platane/snk/svg-only@v3
#         with:
#           github_user_name: ${{ github.repository_owner }}
#           outputs: |
#             dist/github-contribution-grid-snake.svg
#             dist/github-contribution-grid-snake-dark.svg?palette=github-darkcolor_snake=orange
#           # outputs: |
#           #   dark.svg?palette=github-dark&color_snake=blue
#           #   light.svg?color_snake=#141414
#           #   ocean.gif?color_snake=orange&color_dots=#bfd6f6,#8dbdff,#64a1f4,#4b91f1,#3c7dd9
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#       # - name: generate github-contribution-grid-snake.svg
#       #   uses: Platane/snk/svg-only@v3
#       #   with:
#       #     github_user_name: ${{ github.repository_owner }}
#       #     color_snake: 'orange'
#       #     # color_dots: 'green','red','orange','yellow','grey'
#         # env:
#         #   GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
name: generate animation

on:
  # run automatically every 24 hours
  schedule:
    - cron: "0 */12 * * *" 
  
  # allows to manually run the job at any time
  workflow_dispatch:
  
  # run on every push on the main branch
  push:
    branches:
    - main

permissions:
  contents: write
  

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    
    steps:
      # generates a snake game from a github user (<github_user_name>) contributions graph, output a svg animation at <svg_out_path>
      - name: generate github-contribution-grid-snake.svg
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          description: |
            - color_snake: yellow
            - color_dots: #1A1F25,#0ACF83,#1ABCFE,#A259FF,#FF7262
          outputs: |
            dist/github-contribution-grid-snake.svg?color_snake=red&color_dots=#ffff00,#0000ff,#00ffff,#ff0000,#00ff00
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark&color_snake=blue&color_dots=#ffff00,#0000ff,#00ffff,#ff0000,#00ff00
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          
          
      # push the content of <build_dir> to a branch
      # the content will be available at https://raw.githubusercontent.com/<github_user>/<repository>/<target_branch>/<file> , or as github page
      - name: push github-contribution-grid-snake.svg to the output branch
        uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
